from setuptools import setup, find_packages

setup(
    name='python_sony_fcv_ev7520',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
)
